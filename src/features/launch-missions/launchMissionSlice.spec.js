
describe('Create Mission reducer', () => {
    const initialState = {
        mission: {},
        assembles: [],
        launches: [],
        history: []
    };

    it('Should handle default mission state', () => {

    });
});